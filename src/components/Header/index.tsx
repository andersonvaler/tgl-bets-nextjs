import { Container, Logo, Marker } from "./style";

const Header = () => {
  return (
    <Container>
      <Logo>
        TGL
        <Marker />
      </Logo>
    </Container>
  );
};

export default Header;
